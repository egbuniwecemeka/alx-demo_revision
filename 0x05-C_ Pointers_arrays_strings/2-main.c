#include "main.h"

/**
 * main - checks the code
 * 
 * Return: Always 0.
 */

int main(void)
{
    char *str;
    int len;

    str = "Emmanuel";
    len = _strlen(str);
    printf("%d\n", len);

    return (0);
}