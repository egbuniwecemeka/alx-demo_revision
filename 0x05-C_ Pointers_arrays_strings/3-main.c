#include "main.h"

/**
 * main - checks the code
 * 
 * Return: Always 0.
 */

int main(void)
{
    char *str;

    str = "I do not fear the presence of computers, I fear the lack of it";
    _puts(str);

    return (0);
}