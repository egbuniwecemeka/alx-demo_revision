#include "sort.h"

/**
 * main - Checks the code
 * 
 * Return: ) if successful.
*/
int main(void)
{
    int array[] = {2, 6, 78, 45, 33, 22, 1, 7};
    size_t n = sizeof(array) / sizeof(array[0]);

}