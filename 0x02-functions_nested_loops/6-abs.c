#include "main.h"

/**
 * _abs - prints the absolute value of a number
 * an absolute num is a non-negative integar of a num
 * @n: Int value
 *
 * Return: absolute value of int n
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
