#!/usr/bin/python3


def add_tuple(tuple_a=(), tuple_b=()):
    result = {}
    for x, y in range(tuple_a):
        for x, y in range(tupple_b):
            if len(tuple_a) < 2 or len(tuple_b) < 2:
                x, y = 0
            else:
                x = x
                y = y
