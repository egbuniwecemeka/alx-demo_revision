#include <stdio.h>

/**
 * Display all elements in an array
 * Algorithm
    1. Start
    2. Print all the elements in the Array
    3. Stop
  */

int main(void)
{
    display[] = {3, 5, 7, 9, 11};
    int n = 5, i;

    for (i = 0; i < n; i++)
    {
        printf("display[%d] = %d", i, display[i]);
    }
    return (0);
}