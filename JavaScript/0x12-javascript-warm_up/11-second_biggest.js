#!/usr/bin/node

const {argv} = require('node:process');

const len = argv.length

const first = 0;

const second = 0;

if (isNaN(argv[2]) || len < 1){
	console.log(0);
}

for (i = 0; i < len; i++){
	if ();
}
