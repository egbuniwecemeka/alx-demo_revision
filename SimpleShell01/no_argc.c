#include <stdio.h>
#include <stdlib.h>

/**
 * 
 */

int main(int ac, char **av)
{
    int i;

    while (*av != NULL)
    {
        printf("argv[%d] = %s\n", *av);
        av[i];
        i++;
    }
    
}